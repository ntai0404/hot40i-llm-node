# Artifacts

Task evidence, manifests, traces, benchmark outputs and final reports are written here during execution. Large model payloads are not part of the handoff archive and should remain excluded by `.gitignore`.

Canonical per-run evidence layout is defined in `docs/16_EVIDENCE_AND_ARTIFACTS.md` and `schemas/task_evidence.schema.json`.
