# Task backlog

The previous headline backlog has been replaced by the detailed machine-readable execution contract in [`../roadmap/tasks.yaml`](../roadmap/tasks.yaml). Each task there contains dependencies, goal, allowed paths, implementation requirements, verification, required artifacts, pass criteria and failure policy.

Use `python scripts/taskctl.py next` rather than selecting tasks manually.
