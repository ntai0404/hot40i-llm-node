# Claude Code repository instructions

Follow `AGENTS.md` as the binding repository contract. Start from `HANDOFF.md` and `PROJECT_SPEC.md`, then use `scripts/taskctl.py` continuously until `FINAL_DEPLOYMENT` passes or only genuine external blockers remain. Do not reinterpret Claude-specific defaults as permission to bypass device-safety rules.
