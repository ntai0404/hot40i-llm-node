# Benchmarks

Human-readable benchmark summaries may be kept here. Machine-readable run evidence belongs under `artifacts/runs/` and must conform to the schemas and benchmark specification.

See `docs/04_BENCHMARK_SPEC.md`.
